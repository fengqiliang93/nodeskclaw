---
alwaysApply: true
---

# Plan 自动审查

## 强制规则

**每次创建或修改 Plan 后，必须立即调用 plan-reviewer sub-agent 进行审查。这是强制要求，不是建议，不可跳过。**

Plan 完成后的下一个动作就是拉起 plan-reviewer。不需要等用户手动触发。

## 触发时机

以下任一情况发生后，必须立即触发 plan-reviewer：

- 在 Plan 模式下完成 Plan 输出后
- 在 Agent 模式下创建 todo list 计划后
- 对现有 Plan 做出实质性修改后

## 并行策略

根据 Plan 复杂度，通过多个 Task 工具调用同时拉起 plan-reviewer 实例：

| Plan 规模 | 实例数 | 各实例 prompt 中指定的审查焦点 |
|-----------|--------|-------------------------------|
| ≤3 步骤   | 1      | 全面审查（方向 + 步骤 + 全局） |
| 4-8 步骤  | 2      | ① 方向审查 ② 逐步验证 + 全局检查 |
| >8 步骤   | 3      | ① 方向审查 ② 逐步验证 ③ 全局检查 |

多个实例必须在同一条消息中通过多个 Task 工具调用并行发出，不可串行逐个拉起。

## 审查结果处理

所有 plan-reviewer 实例返回结果后：

1. 汇总所有审查意见
2. 对"必须修正"项立即修改 Plan
3. 将审查结果摘要和修改内容告知用户

## 禁止

- **禁止创建或修改 Plan 后不触发 plan-reviewer**
- **禁止忽略 plan-reviewer 返回的"必须修正"项**
