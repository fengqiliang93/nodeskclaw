apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodeskclaw-admin
  namespace: nodeskclaw-system
  labels:
    app: nodeskclaw-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodeskclaw-admin
  template:
    metadata:
      labels:
        app: nodeskclaw-admin
    spec:
      imagePullSecrets:
        - name: cr-pull-secret
      containers:
        - name: nodeskclaw-admin
          image: <YOUR_REGISTRY>/<YOUR_NAMESPACE>/nodeskclaw-admin:20260227-fd01912
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: nodeskclaw-admin
  namespace: nodeskclaw-system
  labels:
    app: nodeskclaw-admin
spec:
  type: ClusterIP
  selector:
    app: nodeskclaw-admin
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
