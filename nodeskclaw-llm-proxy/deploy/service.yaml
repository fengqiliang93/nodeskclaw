apiVersion: v1
kind: Service
metadata:
  name: nodeskclaw-llm-proxy
  namespace: nodeskclaw-system
  labels:
    app: nodeskclaw-llm-proxy
spec:
  type: ClusterIP
  selector:
    app: nodeskclaw-llm-proxy
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
